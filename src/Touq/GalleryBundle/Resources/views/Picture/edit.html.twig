{% extends '::base.html.twig' %}

{% block body %}
{% form_theme edit_form '::form.html.twig' %}

<h1>Album edit</h1>

<form action="{{ path('picture_multiple_update') }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
    
        <button class="primary btn" type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('album') }}">
            Back to the list
        </a>
    </li>
    <li>r
    </li>
</ul>
{% endblock %}